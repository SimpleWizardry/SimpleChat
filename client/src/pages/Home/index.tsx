import React, {FC} from "react";

const Auth: FC = () => (
    <section className="auth">
        <div className="auth__content">
            HOME
        </div>
    </section>
);

export default Auth;